naftiko: 1.0.0-alpha2
info:
  label: Visa BIN Attribute Sharing Service — Full BIN List
  description: 'Visa BIN Attribute Sharing Service — Full BIN List. 2 operations. Lead operation: Bin Range Details Using
    POST. Self-contained Naftiko capability covering one Visa business surface.'
  tags:
  - Visa
  - Full BIN List
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    VISA_API_KEY: VISA_API_KEY
capability:
  consumes:
  - type: http
    namespace: bin-attribute-sharing-full-bin-list
    baseUri: https://sandbox.api.visa.com
    description: Visa BIN Attribute Sharing Service — Full BIN List business capability. Self-contained, no shared references.
    resources:
    - name: v1-binRangeDetails
      path: /v1/binRangeDetails
      operations:
      - name: binrangedetailsusingpostv1
        method: POST
        description: Bin Range Details Using POST
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
    - name: v2-binRangeDetails
      path: /v2/binRangeDetails
      operations:
      - name: binrangedetailsusingpostv2
        method: POST
        description: Bin Range Details Using POST V2
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
  exposes:
  - type: rest
    namespace: bin-attribute-sharing-full-bin-list-rest
    port: 8080
    description: REST adapter for Visa BIN Attribute Sharing Service — Full BIN List. One Spectral-compliant resource per
      consumed operation, prefixed with /v1.
    resources:
    - path: /v1/v1/binrangedetails
      name: v1-binrangedetails
      description: REST surface for v1-binRangeDetails.
      operations:
      - method: POST
        name: binrangedetailsusingpostv1
        description: Bin Range Details Using POST
        call: bin-attribute-sharing-full-bin-list.binrangedetailsusingpostv1
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
    - path: /v1/v2/binrangedetails
      name: v2-binrangedetails
      description: REST surface for v2-binRangeDetails.
      operations:
      - method: POST
        name: binrangedetailsusingpostv2
        description: Bin Range Details Using POST V2
        call: bin-attribute-sharing-full-bin-list.binrangedetailsusingpostv2
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: bin-attribute-sharing-full-bin-list-mcp
    port: 9090
    transport: http
    description: MCP adapter for Visa BIN Attribute Sharing Service — Full BIN List. One tool per consumed operation, routed
      inline through this capability's consumes block.
    tools:
    - name: bin-range-details-using-post
      description: Bin Range Details Using POST
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: bin-attribute-sharing-full-bin-list.binrangedetailsusingpostv1
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
    - name: bin-range-details-using-post-2
      description: Bin Range Details Using POST V2
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: bin-attribute-sharing-full-bin-list.binrangedetailsusingpostv2
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
