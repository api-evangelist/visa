naftiko: 1.0.0-alpha2
info:
  label: Visa Business Data Solutions — Push Authorization Data API
  description: 'Visa Business Data Solutions — Push Authorization Data API. 1 operations. Lead operation: Send Auth Data.
    Self-contained Naftiko capability covering one Visa business surface.'
  tags:
  - Visa
  - Push Authorization Data API
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    VISA_API_KEY: VISA_API_KEY
capability:
  consumes:
  - type: http
    namespace: business-data-solutions-push-authorization-data-api
    baseUri: https://sandbox.api.visa.com
    description: Visa Business Data Solutions — Push Authorization Data API business capability. Self-contained, no shared
      references.
    resources:
    - name: Any_Client_URL
      path: /Any_Client_URL
      operations:
      - name: sendauthdatausingpost
        method: POST
        description: Send Auth Data
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: false
  exposes:
  - type: rest
    namespace: business-data-solutions-push-authorization-data-api-rest
    port: 8080
    description: REST adapter for Visa Business Data Solutions — Push Authorization Data API. One Spectral-compliant resource
      per consumed operation, prefixed with /v1.
    resources:
    - path: /v1/any-client-url
      name: any-client-url
      description: REST surface for Any_Client_URL.
      operations:
      - method: POST
        name: sendauthdatausingpost
        description: Send Auth Data
        call: business-data-solutions-push-authorization-data-api.sendauthdatausingpost
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: business-data-solutions-push-authorization-data-api-mcp
    port: 9090
    transport: http
    description: MCP adapter for Visa Business Data Solutions — Push Authorization Data API. One tool per consumed operation,
      routed inline through this capability's consumes block.
    tools:
    - name: send-auth-data
      description: Send Auth Data
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: business-data-solutions-push-authorization-data-api.sendauthdatausingpost
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
